<UserControl x:Class="LinksAndMore.Views.EditItemDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:models="clr-namespace:LinksAndMore.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <StackPanel Margin="0,10,0,10">
        <ui:TextBlock Text="Type" Margin="0,0,0,5" />
        <ComboBox x:Name="TypeComboBox" Margin="0,0,0,15" HorizontalAlignment="Stretch">
            <ComboBoxItem Content="Link" Tag="{x:Static models:ItemType.Link}" IsSelected="True" />
            <ComboBoxItem Content="Note" Tag="{x:Static models:ItemType.Note}" />
            <ComboBoxItem Content="Snippet" Tag="{x:Static models:ItemType.Snippet}" />
        </ComboBox>

        <ui:TextBlock Text="Category" Margin="0,0,0,5" />
        <ComboBox x:Name="CategoryComboBox" Margin="0,0,0,15" HorizontalAlignment="Stretch" DisplayMemberPath="Name" />

        <ui:TextBlock Text="Title" Margin="0,0,0,5" />
        <ComboBox x:Name="TitleComboBox" IsEditable="True" Margin="0,0,0,15" HorizontalAlignment="Stretch" />

        <ui:TextBlock x:Name="ContentLabel" Text="URL / Content" Margin="0,0,0,5" />
        <ui:TextBox x:Name="ContentTextBox" PlaceholderText="Enter URL or content..." 
                    AcceptsReturn="True" MinHeight="60" Margin="0,0,0,15" />

        <ui:TextBlock Text="Description (Optional)" Margin="0,0,0,5" />
        <ui:TextBox x:Name="DescriptionTextBox" PlaceholderText="Enter description..." 
                    AcceptsReturn="True" MinHeight="60" />
    </StackPanel>
</UserControl>
